<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="edu.hexa.leejaehoon.projectinfo">
	
	<select id="selectProject" resultType="edu.hexa.leejaehoon.domain.ProjectInfoVO">
		select pid, category, pname, intro, start, end 
	from pentacore.project 
	where pid = (select pid from pentacore.project_leader where leader_uid =#{uid});
	</select>
	

</mapper>